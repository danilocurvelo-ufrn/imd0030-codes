#include <vector>

int main() {
   std::vector<int> codigos;              // Vetor de inteiros
   std::vector<int>::iterator it;         // Iterador sobre o vetor de inteiros
   
   for (it = codigos.begin(); it != codigos.end(); ++it) {
      // Iteracao sobre os elementos do vetor utilizando o iterador it
   }

   return 0;
}
